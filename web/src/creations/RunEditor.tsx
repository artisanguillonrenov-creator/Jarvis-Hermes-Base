import type { CreationDraft } from "./types";
export function RunEditor({ draft, onChange }: { draft: CreationDraft; onChange: (d: Partial<CreationDraft>) => void }) { return <section className="creations-card"><div className="creations-section-head"><div><span className="creations-eyebrow">CONFIGURAÇÃO</span><h2>Entradas e saídas</h2></div></div><label className="creations-field">Saídas a executar<input aria-label="IDs das saídas" value={draft.outputNodeIds.join(", ")} onChange={(e) => onChange({ outputNodeIds: e.target.value.split(",").map((x) => x.trim()).filter(Boolean) })} placeholder="node-id-1, node-id-2" /></label><label className="creations-field">Prompt / instrução<textarea aria-label="Prompt" value={draft.overrides.prompt?.prompt ?? ""} onChange={(e) => onChange({ overrides: { ...draft.overrides, prompt: { prompt: e.target.value } } })} placeholder="Edite a instrução desta execução" /></label><p className="creations-muted">O espaço salvo permanece intacto. Alterações aqui valem somente para esta execução.</p></section>; }
