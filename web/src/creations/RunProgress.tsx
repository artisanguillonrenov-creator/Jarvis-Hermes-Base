import type { Run } from "./contracts";
export function RunProgress({ run, onRetry }: { run: Run; onRetry?: (nodeId: string) => void }) { return <section className="creations-card"><div className="creations-section-head"><div><span className="creations-eyebrow">EXECUÇÃO</span><h2>{run.spaceName}</h2></div><strong>{run.state}</strong></div><ol className="creations-steps">{run.steps.map((s) => <li key={s.nodeId} className={`step-${s.state}`}><div><strong>{s.nodeId}</strong><span>{s.model} · {s.provider}</span></div><em>{s.state}</em>{s.error && <p role="alert">{s.error}</p>}{s.state === "error" && onRetry && <button type="button" onClick={() => onRetry(s.nodeId)}>Tentar etapa novamente</button>}</li>)}</ol>{run.state === "reconciliation_required" && <p role="alert" className="creations-warning">Resultado do envio desconhecido. O provedor pode ter aceitado; não reenviamos automaticamente.</p>}</section>; }
